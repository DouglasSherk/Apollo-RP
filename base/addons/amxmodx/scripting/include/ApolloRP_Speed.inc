#if defined _ApolloRP_Speed_included
	#endinput
#endif

#define _ApolloRP_Speed_included

#include <ApolloRP>

native ARP_SetUserSpeed(id,Speed:mode,Float:value = 0.0)

native Float:ARP_GetUserSpeed(id,Speed:mode)